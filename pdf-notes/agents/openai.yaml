interface:
  display_name: "PDF Notes"
  short_description: "Create customer notes from Home/Auto PDFs"
  default_prompt: "Use $pdf-notes to make notes for a customer by reading the matching PDF in Downloads and filling NotesTemplate.txt into a new notes text file."

policy:
  allow_implicit_invocation: true
